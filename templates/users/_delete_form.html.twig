<form method="post" action="{{ path('users_delete', {'usersId': user.usersId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.usersId) }}">
    <button class="btn btn-danger"> Effacer</button>
</form>
